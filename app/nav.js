import React from 'react';

// let links = ['Sample'];
//
// let fs = require('fs');
//
// let files = fs.readdirSync('./*.md', function (err, files) {
//   links.push(files);
// });
//
// console.log(links);

let NavLinks = React.createClass({

  render() {

    return(
      <section id="about">

      </section>
    );
  }
});

export default NavLinks;
